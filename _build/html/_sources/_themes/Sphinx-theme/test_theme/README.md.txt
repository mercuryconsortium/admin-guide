`Gruntfile.js` will copy the generated html and css files to this folder (`test_theme`). 

After you finish testing, copy the contents of `test_theme` to a subdir of `sphinx_theme`, and update `__init__.py` to include the new theme. 
